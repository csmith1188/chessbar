# ChessBar

## Setting up your own instance of Chessbar
1. In the command prompt, run `init.bat`.
2. In `INFO.js`, fill out the correct information.

### If you want to reset the database without resetting `INFO`, run `reset-db.bat`